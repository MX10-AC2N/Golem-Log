name: Build and Deploy

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

# Autorise le workflow à écrire dans les pages GitHub
permissions:
  contents: read # Nécessaire pour lire le code source
  pages: write   # Nécessaire pour écrire sur la branche gh-pages via l'action de déploiement
  id-token: write # Nécessaire pour authentification OIDC

# Permet un seul déploiement à la fois
concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        persist-credentials: false # Important pour éviter les conflits avec deploy-pages
        fetch-depth: 0            # Nécessaire pour trunk et potentialités futures

    - name: Install Rust
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: wasm32-unknown-unknown

    - name: Install Trunk
      uses: jetli/trunk-action@v0.5.0
      with:
        version: 'latest'

    - name: Cache Trunk build directory
      uses: actions/cache@v4
      with:
        path: |
          ~/.cache/trunk
        key: ${{ runner.os }}-trunk-${{ hashFiles('**/Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-trunk-

    - name: Build with Trunk
      run: trunk build --release --public-url ./

    # --- ÉTAPE CRUCIALE : Créer .nojekyll dans dist ---
    - name: Create .nojekyll file in dist
      run: touch dist/.nojekyll

    # --- ÉTAPE CRUCIALE : Télécharger l'artefact pour le déploiement ---
    # Utilisez actions/upload-pages-artifact pour le job deploy
    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: dist # Le dossier généré par trunk, contenant index.html, .wasm, .js, .nojekyll

  # Job de déploiement
  deploy:
    needs: build # Dépend du job build
    runs-on: ubuntu-latest
    # --- ÉTAPE CRUCIALE : Environnement github-pages ---
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
    # --- ÉTAPE CRUCIALE : Déployer l'artefact ---
    # Cette action télécharge automatiquement l'artefact uploadé par actions/upload-pages-artifact
    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
      # Pas besoin de spécifier de chemin, il utilise l'artefact uploadé
      # Par défaut, il publie sur la branche configurée dans les paramètres du dépôt
      # ou sur la branche 'gh-pages' si 'Deploy from a branch' est sélectionné avec 'gh-pages'.
